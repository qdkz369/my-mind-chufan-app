# 决策约束历史快照（原 .backup/constraints）

本目录为**瘦身计划 V2** 从 `.backup/constraints/` 移动而来的历史约束备份，不参与构建与运行。

- **用途**：保留原 decision-budget、design-behavior-lint 等约束相关备份，未被当前平台策略加载、未被 import、未被 migrations 引用。
- **冻结原因**：根目录瘦身；以原始文本保留便于极少再查阅时仍可搜索。

如需恢复或对比，直接在此目录查阅即可。
