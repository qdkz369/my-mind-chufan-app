# 零散 SQL 归档

本目录为**瘦身计划 V2** 冻结的、未纳入 `migrations/` 的零散 SQL 脚本，不参与构建与运行。

- **用途**：保留根目录曾散落的诊断、验证、一次性建表等 SQL，便于后续查阅。
- **冻结原因**：正式迁移以 `migrations/` 为准；此处仅为历史脚本归档，可 grep / code search。

如需恢复某文件到根目录或纳入 migrations，直接移动即可。
