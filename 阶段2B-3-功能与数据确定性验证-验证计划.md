# 阶段 2B-3：功能与数据确定性验证 - 验证计划

**验证日期**：2025-01-20  
**验证目标**：确认系统是确定可运行的，为下一阶段权限迁移提供安全基础

---

## 📋 验证范围

### 总体目标
通过最小但关键的功能测试 + 数据校验，确认：
1. ✅ 新表结构可正常读写
2. ✅ 主干业务流程无阻断
3. ✅ 为下一阶段「Service Role → Anon + RLS 权限迁移」提供安全基础

---

## 任务一：功能主干路径测试

### 【报修工单 repair_orders】

#### 1. 创建报修工单

**测试步骤**：
```bash
POST /api/repair/create
Content-Type: application/json

{
  "restaurant_id": "<测试餐厅ID>",
  "service_type": "维修服务",  // 或 "清洁服务" / "工程改造"
  "description": "测试报修工单描述",
  "urgency": "medium",
  "contact_phone": "13800138000"
}
```

**验证点**：
- [ ] API 返回 `success: true`
- [ ] 返回数据包含 `id`, `restaurant_id`, `service_type`, `status`
- [ ] 数据库中 `repair_orders` 表有对应记录
- [ ] `status` 为 `pending`
- [ ] `service_type` 正确

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

**备注**：记录返回的 `id`，用于后续测试

---

#### 2. 查询报修工单列表

**测试步骤**：
```bash
GET /api/repair/list
```

**验证点**：
- [ ] API 返回 `success: true`
- [ ] 返回数据包含刚创建的报修工单（通过 `id` 确认）
- [ ] 列表数据格式正确（包含 `id`, `restaurant_id`, `service_type`, `status` 等字段）
- [ ] 可以按 `status` 筛选（如 `?status=pending`）

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

---

#### 3. 更新报修工单状态

**测试步骤**：
```bash
POST /api/repair/update
Content-Type: application/json

{
  "id": "<步骤1创建的工单ID>",
  "status": "processing"  // 或 "completed" / "cancelled"
}
```

**验证点**：
- [ ] API 返回 `success: true`
- [ ] 数据库中 `repair_orders` 表的 `status` 已更新
- [ ] 可以成功更新为 `completed`（需提供 `amount`）
- [ ] `updated_at` 字段已更新

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

---

### 【燃料配送 delivery_orders】

#### 4. 创建燃料配送订单

**测试步骤**：
```bash
POST /api/orders/create
Content-Type: application/json

{
  "restaurant_id": "<测试餐厅ID>",
  "product_type": "lpg",  // 或 "clean" / "alcohol"
  "amount": 100.00,
  "status": "pending"
}
```

**验证点**：
- [ ] API 返回 `success: true`
- [ ] 返回数据包含 `id`, `restaurant_id`, `product_type`, `service_type`
- [ ] 数据库中 `delivery_orders` 表有对应记录
- [ ] `service_type` 自动设置为 `"燃料配送"`
- [ ] `status` 为 `pending`

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

**备注**：记录返回的 `id`，用于后续测试

---

#### 5. 查询待接单列表

**测试步骤**：
```bash
GET /api/orders/pending
```

**验证点**：
- [ ] API 返回 `success: true`
- [ ] 返回数据包含刚创建的配送订单（通过 `id` 确认）
- [ ] 列表数据格式正确
- [ ] 可以按 `product_type` 筛选（如 `?product_type=lpg`）

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

---

#### 6. 执行接单 / 派单 / 完成流程

**测试步骤 A：接单**
```bash
POST /api/orders/accept
Content-Type: application/json

{
  "order_id": "<步骤4创建的订单ID>",
  "worker_id": "<测试配送员ID>"
}
```

**测试步骤 B：派单**
```bash
POST /api/orders/dispatch
Content-Type: application/json

{
  "orderId": "<步骤4创建的订单ID>",
  "worker_id": "<测试配送员ID>"
}
```

**测试步骤 C：完成**
```bash
POST /api/orders/complete
Content-Type: application/json

{
  "order_id": "<步骤4创建的订单ID>",
  "tracking_code": "TEST-001",
  "proof_image": "https://example.com/proof.jpg"
}
```

**验证点**：
- [ ] 接单：`status` 更新为 `active`，`assigned_to` 和 `worker_id` 已设置
- [ ] 派单：`status` 更新为 `delivering`，`assigned_to` 和 `worker_id` 已设置
- [ ] 完成：`status` 更新为 `completed`，`tracking_code` 和 `proof_image` 已设置
- [ ] 数据库中 `delivery_orders` 表的相应字段已更新

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

---

### 【跨表逻辑】

#### 7. 支付回调逻辑

**测试步骤**：
```bash
POST /api/payment/alipay/notify
Content-Type: application/x-www-form-urlencoded

out_trade_no=<订单ID>&trade_status=TRADE_SUCCESS
```

**验证点**：
- [ ] API 返回 `success`（200 状态码）
- [ ] 如果订单ID在 `delivery_orders` 中，`delivery_orders` 表的 `status` 更新为 `paid`
- [ ] 如果订单ID在 `repair_orders` 中，`repair_orders` 表的 `status` 更新为 `paid`
- [ ] `updated_at` 字段已更新

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

**备注**：需要测试两种情况：
- 测试 `delivery_orders` 的支付回调
- 测试 `repair_orders` 的支付回调

---

#### 8. 管理后台 dashboard

**测试步骤**：
1. 访问管理后台：`/dashboard`
2. 查看订单列表
3. 检查排序和筛选功能

**验证点**：
- [ ] 页面能正常加载
- [ ] 能看到 `repair_orders` 的数据
- [ ] 能看到 `delivery_orders` 的数据
- [ ] 两个表的数据能正确合并显示
- [ ] 排序功能正常（按 `created_at` 降序）
- [ ] 筛选功能正常（按 `status` 筛选）
- [ ] 列表无明显异常（无重复、无缺失）

**预期结果**：✅ 成功 / ❌ 失败 / ⚠️ 异常

---

## 任务二：数据一致性验证（SQL 层）

### SQL 验证查询

#### 1. 统计新表数据量

```sql
-- 统计 repair_orders 表数据量
SELECT COUNT(*) as repair_orders_count FROM repair_orders;

-- 统计 delivery_orders 表数据量
SELECT COUNT(*) as delivery_orders_count FROM delivery_orders;
```

**验证点**：
- [ ] `repair_orders_count` > 0（如果有历史数据）
- [ ] `delivery_orders_count` > 0（如果有历史数据）
- [ ] 数量合理（无异常大量数据）

**预期结果**：✅ 一致 / ❌ 异常

---

#### 2. 对比原 orders 表拆分逻辑

```sql
-- 统计原 orders 表中应迁移到 repair_orders 的数据
SELECT 
  COUNT(*) FILTER (WHERE service_type IN ('维修服务', '清洁服务', '工程改造')) AS repair_count,
  COUNT(*) FILTER (WHERE service_type = '燃料配送') AS delivery_count
FROM orders;

-- 对比新表数据量
SELECT 
  (SELECT COUNT(*) FROM repair_orders) AS repair_orders_count,
  (SELECT COUNT(*) FROM delivery_orders) AS delivery_orders_count,
  (SELECT COUNT(*) FILTER (WHERE service_type IN ('维修服务', '清洁服务', '工程改造')) FROM orders) AS orders_repair_count,
  (SELECT COUNT(*) FILTER (WHERE service_type = '燃料配送') FROM orders) AS orders_delivery_count;
```

**验证点**：
- [ ] `repair_orders_count` ≈ `orders_repair_count`（允许少量差异，因为可能有新数据）
- [ ] `delivery_orders_count` ≈ `orders_delivery_count`（允许少量差异，因为可能有新数据）
- [ ] 数量差异在合理范围内（< 10%）

**预期结果**：✅ 一致 / ❌ 不一致

---

#### 3. 判断是否存在明显异常

```sql
-- 检查 repair_orders 表中的 NULL 值
SELECT 
  COUNT(*) as total,
  COUNT(*) FILTER (WHERE restaurant_id IS NULL) as null_restaurant_id,
  COUNT(*) FILTER (WHERE service_type IS NULL) as null_service_type,
  COUNT(*) FILTER (WHERE status IS NULL) as null_status
FROM repair_orders;

-- 检查 delivery_orders 表中的 NULL 值
SELECT 
  COUNT(*) as total,
  COUNT(*) FILTER (WHERE restaurant_id IS NULL) as null_restaurant_id,
  COUNT(*) FILTER (WHERE service_type IS NULL) as null_service_type,
  COUNT(*) FILTER (WHERE status IS NULL) as null_status
FROM delivery_orders;

-- 检查数据完整性（关键字段）
SELECT 
  'repair_orders' as table_name,
  COUNT(*) as total,
  COUNT(DISTINCT restaurant_id) as unique_restaurants,
  COUNT(DISTINCT service_type) as unique_service_types,
  COUNT(DISTINCT status) as unique_statuses
FROM repair_orders
UNION ALL
SELECT 
  'delivery_orders' as table_name,
  COUNT(*) as total,
  COUNT(DISTINCT restaurant_id) as unique_restaurants,
  COUNT(DISTINCT service_type) as unique_service_types,
  COUNT(DISTINCT status) as unique_statuses
FROM delivery_orders;
```

**验证点**：
- [ ] 关键字段（`restaurant_id`, `service_type`, `status`）的 NULL 值数量为 0 或极少
- [ ] 数据分布合理（`unique_restaurants`, `unique_service_types`, `unique_statuses` 正常）
- [ ] 无大量失败写入（通过对比原表和新表数量判断）

**预期结果**：✅ 正常 / ❌ 异常

---

## 任务三：风险扫描（只读，不改结构）

### 1. 检查日志中是否存在错误

**检查点**：
- [ ] `relation does not exist` - 表不存在错误
- [ ] `permission denied` - 权限拒绝错误
- [ ] `RLS violation` / `row-level security` - RLS 策略违反错误

**检查方法**：
1. 查看服务器日志（Next.js 控制台输出）
2. 查看 Supabase Dashboard 的日志
3. 检查浏览器控制台错误

**预期结果**：✅ 无错误 / ⚠️ 有警告 / ❌ 有错误

---

### 2. 确认 Service Role Key 使用的 API 是否能正常工作

**需要验证的 API**（使用 Service Role Key）：
- [ ] `app/api/admin/create-company/route.ts` - 创建供应商公司
- [ ] `app/api/admin/find-user/route.ts` - 查找用户

**验证点**：
- [ ] API 能正常调用
- [ ] 返回预期结果
- [ ] 无权限错误

**预期结果**：✅ 正常 / ❌ 异常

---

### 3. 确认未提前破坏权限冻结策略

**检查点**：
- [ ] 所有 API 文件顶部的权限标注注释仍然存在
- [ ] 没有意外删除或修改权限标注
- [ ] 权限边界冻结策略未被破坏

**检查方法**：
```bash
# 检查所有 API 文件是否包含权限标注
grep -r "ACCESS_LEVEL:" app/api/
```

**预期结果**：✅ 正常 / ❌ 异常

---

## 📝 验证结果记录模板

### 功能测试结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 1. 创建报修工单 | ⬜ | |
| 2. 查询报修工单列表 | ⬜ | |
| 3. 更新报修工单状态 | ⬜ | |
| 4. 创建燃料配送订单 | ⬜ | |
| 5. 查询待接单列表 | ⬜ | |
| 6. 执行接单/派单/完成流程 | ⬜ | |
| 7. 支付回调逻辑 | ⬜ | |
| 8. 管理后台 dashboard | ⬜ | |

### 数据一致性验证结果

| 验证项 | 状态 | 数据 | 备注 |
|--------|------|------|------|
| repair_orders 数据量 | ⬜ | | |
| delivery_orders 数据量 | ⬜ | | |
| 数据迁移一致性 | ⬜ | | |
| 数据完整性检查 | ⬜ | | |

### 风险扫描结果

| 检查项 | 状态 | 发现的问题 | 备注 |
|--------|------|-----------|------|
| 日志错误检查 | ⬜ | | |
| Service Role Key API | ⬜ | | |
| 权限冻结策略 | ⬜ | | |

---

## ⚠️ 注意事项

1. **不要新增表**：只验证现有表结构
2. **不要修改 RLS 策略**：只验证现有策略
3. **不要重构代码**：只验证现有代码
4. **不要提前迁移 Service Role → Anon Key**：这是下一阶段的工作

---

## 📊 验证结论

**是否可以进入下一阶段【阶段 2B-4：权限迁移】？**

- [ ] ✅ 是 - 所有验证通过，可以进入下一阶段
- [ ] ❌ 否 - 存在阻塞问题，需要先解决
- [ ] ⚠️ 部分通过 - 部分问题需要解决，但不阻塞下一阶段

**结论说明**：

---

**验证完成日期**：  
**验证人员**：  
**验证版本**：v1.0
